# V2 部署使用说明

## 🎯 部署策略

### 基础框架 (01, 02, 03) - 只部署一次
- `01_Deploy_Templates.ts` - 部署模板合约
- `02_Deploy_Factories.ts` - 部署工厂合约  
- `03_Deploy_Creation.ts` - 部署Creation合约

### 项目脚本 (04, 05) - 可多次执行
- `04_Deploy_Example_Project.ts` - 部署示例项目
- `05_Start_Invest.ts` - 启动投资流程

## 🚀 使用方法

### 方法一：使用部署管理脚本（推荐）

```bash
# 1. 首次部署基础框架（只执行一次）
node scripts/deploy-v2.js infrastructure baseSepolia

# 2. 部署项目（可多次执行）
node scripts/deploy-v2.js project baseSepolia

# 3. 启动投资流程（可多次执行）
node scripts/deploy-v2.js invest baseSepolia

# 4. 一键部署所有（基础框架 + 项目）
node scripts/deploy-v2.js all baseSepolia

# 5. 重置部署状态（重新开始）
node scripts/deploy-v2.js reset baseSepolia
```

### 方法二：直接使用 hardhat 命令

```bash
# 1. 首次部署基础框架（只执行一次）
npx hardhat deploy --tags v2-infrastructure --network baseSepolia

# 2. 部署项目（可多次执行）
npx hardhat run deploy/v2/04_Deploy_Example_Project.ts --network baseSepolia

# 3. 启动投资流程（可多次执行）
npx hardhat run deploy/v2/05_Start_Invest.ts --network baseSepolia
```

## 📋 部署流程

### 首次部署
1. 运行 `node scripts/deploy-v2.js infrastructure baseSepolia`
2. 系统会自动部署 01、02、03 脚本
3. 记录部署状态到 `deployment-status-baseSepolia.json`

### 后续部署
1. 运行 `node scripts/deploy-v2.js project baseSepolia` 部署新项目
2. 运行 `node scripts/deploy-v2.js invest baseSepolia` 启动投资
3. 可以多次执行，每次都会创建新的项目实例

## 🔄 状态管理

### 部署状态文件
- 位置：`deploy/v2/deployment-status-{network}.json`
- 内容：记录基础设施是否已部署、项目列表等

### 重置部署
```bash
# 清除所有部署状态和缓存
node scripts/deploy-v2.js reset baseSepolia

# 然后重新开始部署
node scripts/deploy-v2.js infrastructure baseSepolia
```

## 📝 注意事项

1. **基础框架只部署一次**：01、02、03 脚本只会在首次部署时执行
2. **项目可以多次部署**：04、05 脚本可以多次执行，每次创建新的项目实例
3. **网络配置**：确保在正确的网络上部署（如 baseSepolia）
4. **依赖关系**：项目脚本依赖于基础框架，确保基础框架已部署
5. **测试账户配置**：在 Base Sepolia 等真实网络中，需要配置测试账户私钥才能进行投资测试

## 🎯 最佳实践

1. **开发环境**：可以频繁重置和重新部署进行测试
2. **测试网络**：基础框架部署一次，项目可以多次部署测试
3. **主网**：基础框架部署一次，项目按需部署

## 🔧 故障排除

### 如果部署失败
1. 检查网络连接和配置
2. 检查账户余额和 gas 费用
3. 运行 `node scripts/deploy-v2.js reset baseSepolia` 重置状态
4. 重新开始部署流程

### 如果项目找不到
1. 确保基础框架已部署
2. 检查项目名称是否正确
3. 查看部署日志确认项目创建成功
